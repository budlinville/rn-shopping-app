class CartItem {
	constructor(quantity, productPrice, prouctTitle, sum) {
		this.quantity = quantity;
		this.productPrice = productPrice;
		this.prouctTitle = prouctTitle;
		this.sum = sum;
	}
}

export default CartItem;